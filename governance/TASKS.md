# Mission 9: System Stability & Crash Resilience
*Status: LOCKED*

## Phase A: UI Crash Boundary
- [x] 1. Create `packages/client/src/components/ErrorBoundary.tsx` class component.
- [x] 2. Wrap `<AppContent />` root in `App.tsx` with `<ErrorBoundary>`.

## Phase B: Server Exception & Signal Handling
- [x] 3. Add 4-argument Express error handling middleware `(err, req, res, next)` in `index.ts`.
- [x] 4. Add `unhandledRejection`, `uncaughtException`, `SIGTERM`, `SIGINT` process listeners.

## Phase C: Transport & Safe Parsing
- [x] 5. Create `packages/client/src/lib/api.ts` exporting `API_BASE` and `safeJson<T>()`.
- [x] 6. Create `packages/client/.env.example` documenting `VITE_API_URL`.
- [x] 7. Replace 44 hardcoded `http://` strings across 13 client files with `${API_BASE}`.
- [x] 8. Replace unsafe `.json()` calls with `safeJson(response)`.

## Phase D: Verification & Finalization
- [x] 9. Run full verification suite (`tsc`, `vitest`, `knip`, `playwright`).
- [x] 10. Lock Mission 9 and update `STABILITY_GAP_ANALYSIS.md`.

---

# Missions 10-32: Execution Ledger
*Status: LOCKED (all)*

Atomic task breakdowns for Missions 10-32 were tracked per-mission in `MISSION.md` history and the git commit record, not in this file. This ledger records their lock state and governing commit for traceability (see `ROADMAP.md` for titles).

| Mission | Lock Evidence |
| :--- | :--- |
| M-10 | `1de46b1` |
| M-11 | No lock commit exists anywhere (branches, reflog, dangling objects, full-text search); MISSION.md jumps M-10 → M-14 |
| M-12..M-14 | `1d5ebbe` |
| M-15 | `7416446` |
| M-16..M-17 | `40178f4` |
| M-18 | `843d112` |
| M-19 | `859a428` (MISSION.md: "Governance Framework Maturation (.agents 10/10 MVP)") |
| M-20 | `580219e` |
| M-21 | `54c1370` |
| M-22 | `7335f3e` |
| M-23 | `93e7c16` |
| M-24 | `d79a088` |
| M-25 | `d1592e2` |
| M-26 | `7826ebb` |
| M-27..M-29 | `eaccfe1` |
| M-30 | `a33dedf` |
| M-31 | `7b5cee1` |
| M-32 | `e0bb3bb`, re-verified 2026-08-14 per AFR-002 |
| M-33 | Shifts/users coverage (2026-08-14); shifts 55.6% -> 93.1%, users 38.2% -> 91.7%; closes TD-003/004/005 |
| M-34 | Expenses/auditLogs coverage (2026-08-14); expenses 62.9% -> 92.1% stmts/100% branch, auditLogs 72.7% -> 90.9% stmts/100% branch |
| M-35 | Final-4 coverage (2026-08-14); sales 76.8% -> 91.3%, credits 75.6% -> 92.3%, gameRates 75.0% -> 92.9%, employees 73.3% -> 93.3% (all 100% branch) |
| M-35 (errata) | Per-suite test-count correction (2026-08-15). M-35 MISSION.md recorded `credits 7 -> 14, gameRates 5 -> 10, employees 5 -> 10` (23 tests); committed tree `f5f2dd4` shows credits 17, gameRates 13, employees 13 — true deltas +10/+8/+8 = +36 tests (140 -> 176 total, verified). Coverage percentages unchanged and correct. |
| M-36 | App composition root + schemas coverage (2026-08-15); app.ts 83.3% -> 100% stmts, schemas/index.ts 91.2% -> 100% stmts |
| M-37 | Shifts/users branch gaps (2026-08-16, Locked); shifts branch 88.0%, users branch 97.6%; coverage work shipped inside M-38 commit `5c5256d` |
| M-38 | ACP-004 Dead Guard Removal (2026-08-16, Locked); removed 48 Zod-/auth-shadowed controller guards, `AuthRequest.user` non-optional via Express global augmentation, added `reason` to `UpdateCreditSchema` + `shift_id` to `ResolveMissedDaySchema` (restores stale-shift resolution + credit reason persistence); controllers now 100% stmts/lines, 185 tests pass |
| M-39 | Governance Housekeeping (2026-08-16, Locked); M-37 closure, ROADMAP sync (M-37/M-38/M-39), TD-009 recorded in DEBT.md; `git rm --cached packages/shared/tsconfig.tsbuildinfo` delegated to Product Owner |
| M-40 | Zero-Lint-Warning Cleanup (2026-08-16, Locked); `biome check .` 62 → 0 warnings across 8 controller test files — 21 `path` → `_path` unused-param renames + 41 `as any` suppression comment placements (34 insertions + 7 relocations fixing co-located `suppressions/unused`) |
| M-41 | Governance Housekeeping II (2026-08-16, Locked); evidence-packet versioning (per-mission archive `.agents/evidence_packets/<id>.json` + canonical latest-pointer) + Gate-6 SSOT auto-stamps `Current Mission`/`Mission Status` into SYSTEM_CONTEXT.md (pointer had drifted 7 missions stale) |
| M-42 | Mission Record Standardization (2026-08-16, Locked); lock_mission.sh flips §5 AVP-001 checkbox to `[x]` at lock + warns on missing `**Type:**` field; ENGINEERING_LIFECYCLE.md documents the mandatory `**Type:**` convention (mission_gate.sh ADR-007 requirement); M-42 record models `**Type:** Governance` |
| M-43 | Client Coverage (2026-08-16, Locked); vitest include glob fixed to `packages/**/*.{test,spec}.{ts,tsx}` — un-ghosts `AuthContext`/`ShiftContext` `.tsx` suites (188 → 221 tests, +36 client tests); new suites: lib/api (safeJson, jsdom for module `window` ref), lib/dateUtils (Addis boundaries), lib/utils (cn), components/ErrorBoundary, components/MissedDataBlocker, pages/Login; contexts extended with positive paths; root vitest config gains `@` alias (client `@/src` imports) + v8 coverage gate enforced in lock Gate 3 via `--coverage`; per-directory thresholds (lib 95/95, contexts 70/85, components 30/30) set from measured coverage; pages/layouts deliberately excluded from gating (Playwright e2e coverage), Red-proofed by excluding client tests (all six constraints trip) |
| M-44 | Hook & Script Hardening (2026-08-16, Locked); lock_mission.sh Gate-6 AVP-flip regex hardened from exact string `- [ ] AVP-001 Architecture Verification: pending.` to prefix-anchored `- \[ \] AVP-001 Architecture Verification:.*` (closes M-43 Deferred Decision; Red-proofed: M-43 variant line flips to `[x]`, already-`[x]` lines untouched); lint-staged wired from empty-array no-op to `biome check --write --no-errors-on-unmatched` on `*.{ts,tsx,js,jsx,json}` — pre-commit hook now lints/formats staged source to the M-40 zero-warning standard (previously only tsc + knip gated commits) |
| M-45 | Client Page Unit Coverage — Cohort 1 (2026-08-16, Locked); 5 new page suites (Dashboard, AuditLogs, Expenses, GameSales, Keno) — 38 new tests (221 → 259); pages coverage 1.34% → 42.31% lines / 36.79% functions; vitest gains `packages/client/src/pages/**` threshold {lines 40, functions 35} set empirically with headroom; Red-proofed (page suites excluded → both constraints trip 0% < 40/35); conventions applied: real `Response` objects with JSON content-type (M-43 headerless-mock lesson), `@/src` free page imports, firebase/sonner/context modules mocked at module level, `loading`/`missedData` fields per AuthContext/ShiftContext contracts; Admin, Credits, EmployeeRoster, Reports, SalaryReport + Layout deferred to M-46 |
| M-46 | Client Page Unit Coverage — Cohort 2 (2026-08-17, Locked); 6 new suites (Admin, Credits, EmployeeRoster, Reports, SalaryReport, Layout) — completes client page/layout coverage sweep; pages threshold raised from M-45 floor; new layouts threshold added; recharts mocked for Reports.tsx; Layout gets new `__tests__/layouts/` directory |
| M-47 | App & UserManagement Unit Coverage (2026-08-17, Locked); 2 new suites (App.test.tsx, UserManagement.test.tsx) — covers last 0%-coverage files; components threshold raised from 30/30; explicit vitest imports fix (Gate-2 tsc lesson) |
| M-48 | ACP-005 Pre-Commit Mission Lock (2026-08-17, Active); `.agents/scripts/lock_guard.sh` enforced last-job lock in `.husky/pre-commit` — completes and locks a mission at commit time; closes the M-47 "commit passes, lock fails" failure mode |
| M-49 | Client Coverage — AuthContext/ShiftContext/Credits/EmployeeRoster gaps (2026-08-17, Locked `6658b67`); +26 tests (329 → 355); AuthContext 61.36% → 100% stmts, ShiftContext 86.11% → 100% stmts, Credits 68.80% → 93.6% stmts/95.65% lines, EmployeeRoster 77.24% → 95.12% stmts; contexts {70,85} → {95,90} lines/funcs, pages {80,85} → {85,90}; Red-proofed (4 suites excluded → all four constraints trip) |
| M-50 | Shift Non-Blocking + Backdated Data Entry (2026-08-17, Locked); removed MissedDataBlocker + resolve-missed endpoint; added date pickers to GameSales/Keno/Expenses/Credits; 372 tests pass; ACP-006 + ADR-008 |
| M-61 | UX Polish Batch — Issues #22–#27 (2026-08-21, Locked); `visibilitychange` refetch-on-focus added to GameSales/Keno/Credits/Expenses; central `authFetch` wrapper (client lib/api.ts) with 401→signOut wired across 12 API-calling files / 56 call sites (AuthContext excluded as auth bootstrap); confirmation dialogs for role change (UserManagement) and logout (Layout); audit log documents gain `action` field (shared type + 9 controllers + client display); Expenses submit guard adds `!itemName`; gate-closure test batch (+13: category update/reactivate/cancel-edit flows, qty×unit-price auto-calc, date filters, AuditLogs Load More pagination, Credits employee filter, Keno date field, Layout cancel-signout, 4× visibilitychange refetch) lifted pages coverage over floors (lines 87.96%→90%+, functions 92.35%→93%+) and layouts functions to 100%; 419 tests / 34 files green; depcruise clean; Knip clean; E2E 6/6 |

| M-62 | Resolve TD-027 — Reports netProfit subtracts pending credits (2026-08-21, Locked); Reports.tsx `netProfit` now deducts PENDING credits only (Dashboard-parity semantics; Deducted/Resolved excluded as already recovered); +1 Red-Green-proven test (pending-only subtraction asserted at $40.00 vs unfixed $50.00); suite 419 → 420 tests / 34 files green; tsc + Biome clean; depcruise exit 0 |
| M-63 | Resolve TD-028 — Credit `reason` round-trip & display (2026-08-21, Locked); shared Credit type += optional `reason`; Credits.tsx reason input (create/edit), row display, state-reset hygiene; SalaryReport deduction rows show reason; server zero-change (verified payloads); +3 Red-Green tests; suite 420 → 423 / 34 files green; tsc, Biome, knip, depcruise clean |
| M-64 | Resolve TD-042 — invite form label (2026-08-21, Locked); UserManagement CardTitle "Invite Employee" → "Invite User" (PO decision: employees are workflow records, not users); +1 Red-Green regression test; suite 423 → 424 / 34 files green; tsc + Biome clean |
| M-65 | Resolve TD-014 — server error message sanitization (2026-08-21); new `utils/safeError.ts` allowlist sanitizer (sentinel messages pass, all else → "Internal server error"); 30 controller catch-fallbacks converted across 10 controllers; intentional sentinel mappings (DUPLICATE_NAME/not-found/forbidden) preserved; leak-pinned test assertions updated ("DB crashed" toBe/toContain pins → generic); +1 Red-Green-proven leak test (failed pre-fix exposing `projects/secret` Firestore path, passes post-fix); 0 echo paths remain (grep-verified); suite 424 → 425 / 34 files green; tsc, Biome (src clean; test-file noExplicitAny warnings are pre-existing convention), knip, depcruise clean |
| M-66 | Keno Direct Net Entry (2026-08-21, PO-approved design: conditional historical display + net-only strict contract); client form Sales/Payouts pair → single `Net Amount ($)` input (Keno.tsx), edit prefill from net_profit, live-preview echoes entry; server `CreateKenoSchema`/`UpdateKenoSchema` → net-only `.strict()` (legacy-shaped payloads 400), `createKeno` persists net_profit only; shared `KenoLog.sales/payouts` optional; Keno.tsx history rows conditional (legacy shows Sales/Payouts, net-only rows omit); Reports.tsx keno table em-dash cells for missing values (columns retained); tests: server suite payload migration + strict-rejection negative test (suite discriminates old/new contract — Red captured mid-change: 7 server failures against legacy payloads), client Keno rewrite + conditional-render test (15→16), Reports net-only row test (+1); 425 → 427 / 34 files green; tsc=0, knip=0, depcruise config-mode clean, Biome src clean. Hotfix (same mission): runtime `null.toFixed` crash on entries carrying explicit `null` sales/payouts (console-authored data) — guards `!== undefined` pass `null`; switched to `!= null` in Keno.tsx/Reports.tsx + null-fields regression fixture; RCA'd before mutation, suite re-certified 427/427. Review pass (2026-08-21): updateKeno dead sales/payouts branches removed (M-38 precedent); strict rejection empirically probed via transient tsx scripts ("Unrecognized key(s): 'sales','payouts'"); +1 PUT strict-rejection test → 428/428; blast radius git-verified to exactly the 10 mission paths (docs/reports/M-66_Blast_Radius_Report.md). Residual resolution (PO-approved): converge-on-edit — updateKeno FieldValue.delete()s stored sales/payouts on any edit (originals persist in audit old_value; +1 payload-capture test; RCA'd test-isolation failure: post-transaction re-read inherited stale db.collection mock → self-contained stub); scoped schema refinement — passthrough+superRefine yields explicit "Retired field(s)" 400 message for sales/payouts while unknown typos keep generic wording (+message assertions on POST/PUT negatives); suite 428 → 429 / 34 files green; all gates re-certified. Production hotfix (2026-08-22, post-M-67): runtime keno edits failed with generic "Failed to save keno log" toast — RCA: converge-on-edit spread `FieldValue.delete()` sentinels from `newValues` into the audit `transaction.set()` payload; installed SDK rejects delete sentinels in plain set() (empirically probed: "must only be used in update() or set() with {merge:true}") → every edit threw → 500 → masked by safeErrorMessage; mock-based tests blind (vi.fn accepts anything). Fix: audit `new_value` now records the converged net-only shape (sentinel-free copy of oldDoc minus sales/payouts, plus applied net_profit); legal update() sentinels untouched; originals preserved in old_value. Red-Green: +1 audit-capture test failed pre-fix (sentinel scan via instanceof FieldValue), passed post-fix → 429 → 436 / 34 files; tsc=0, knip=0, depcruise ✔, Biome=0 |
| M-67 | Resolve TD-046 — Game-log history browsing on entry pages (2026-08-22, PO-approved Plan A: range pickers defaulting to shop-day today); Keno.tsx replaces fetch-all+client-filter with ranged GET (`startDate`/`endDate` via `getShopStartOfDay/EndOfDay(pickedDate)`) + From/To inputs + Apply + period-aware description/empty state; GameSales.tsx same range controls over existing ranged query, monolithic loader split (rates mount-once, sales logs ranged callback); server source untouched (endpoints already ranged — ADR-001 upheld); tests +8: 2 behavioral range-exclusion server tests per keno/sales (mock Firestore filters only on captured `where` bounds; out-of-range doc must be absent), client initial-load param assertions + Apply-refetch flows asserting exact encoded bounds computed via shared dateUtils (tz-safe) + range wording swap; suite 429 → 435 / 34 files green (server suites re-run 43/43 post-hardening: call-shape assertions upgraded to behavioral exclusion per Rule 28); tsc=0, knip=0, depcruise no-violations (147 modules, 439 deps), Biome all touched=0; blast radius git-verified to exactly the 8 mission paths (docs/reports/M-67_Blast_Radius_Report.md). Review pass (2026-08-22): containment re-verified (10 paths incl. governance+report; zero server-src changes; +4 depcruise edges = new test imports); empirical probes — collection constants match test literals, both controllers persist `date` as uniform ISO strings (lexicographic range comparison sound), DateRangeQuerySchema accepts encoded bounds / rejects non-strings, handlers decoupled from hoisted loaders via local setLogs (no stale-closure surface). Finding documented not changed: dateUtils bounds are host-TZ-sensitive (`startOfDay(toZonedTime(...))` truncates via host-local getters) — exact Addis day under TZ=Africa/Addis_Ababa (probed byte-equal to Reports' inline +03:00), +3h-shifted window under UTC hosts; trait predates M-67 (all no-arg "today" usages share it), shop browsers unaffected; candidate future proposal to normalize host-independence (PO approval required). Minor pre-existing notes: create/edit prepends without range re-check; GameSales loadingRates cleared by first-finishing loader. Post-review: 435/435, tsc=0, Biome=0, footprint unchanged |
| M-68 | Entry-page hardening — UI/UX gaps A1/B1/B2/C1/C2 (2026-08-22, PO-selected from docs/reports/UIUX_GAP_ANALYSIS_GAMESALES_KENO_2026.md); A1: server `finiteNumber` zod helper (null/""→NaN preprocess + isFinite refine; negatives legal) on keno create/update net_profit + client pure `parseNetAmountInput` (Number() full-string semantics mirroring z.coerce — caught & fixed parseFloat truncation hole "1e-"→1 during Red-Green); B1: Keno rows adopt "MMM d, h:mm a"; B2: range summaries ("N entries · Net $X" / "N sales · Total $X") from displayed logs on both pages; C1: verifyingId/deletePending in-flight guards, sibling row actions disabled while pending, ConfirmDialog loading prop (existing API reused); C2: all fetch catch sites toast err.message with generic fallbacks (server text M-65-sanitized), 9 existing toast assertions migrated to surfaced messages. Rule-28 note: jsdom sanitizes number inputs making integration guard test vacuous → extracted pure helper + table-driven unit tests instead; server Reds captured pre-fix (3 coerce-to-0 failures). 436 → 444/444 / 34 files; tsc=0, knip=0, depcruise ✔ (147 modules), Biome=0. Review pass (2026-08-22): containment re-verified (10 paths; finiteNumber keno-only; helper single-consumer; ConfirmDialog unmodified); empirical schema matrix 13/13 incl. `.optional()` short-circuit proof (reason-only PUTs unaffected) and new server-side rejection of non-numeric strings; C1 double-confirm impossibility verified at ConfirmDialog source (loading disables both buttons); C2 sweep found one missed fetch-catch site (GS default-games banner) — fixed in-turn + its pre-existing test migrated to surfaced message; leftover sweeps clean (no parseFloat(netAmount)/bare h:mm); pre-existing Expenses.test.tsx lint debt at HEAD documented via stdin-probe (outside blast radius). Post-review: 444/444, tsc=0, knip=0, depcruise ✔, Biome touched=0 |
| M-69 | Keno history beside entry form (2026-08-22, PO layout-parity request); Keno.tsx stacked max-w cards → GameSales-mirroring `grid grid-cols-1 lg:grid-cols-3 gap-6` wrapper (form `lg:col-span-1 h-fit`, history `lg:col-span-2`), mobile stacking preserved; presentation-only — zero logic/contract/test changes; 444/444 / 34 files; tsc=0, Biome=0 |
| M-70 | Keno history UX redesign (2026-08-22, PO request): day-grouped sections with weekday+date headers (`EEE, MMM d`) and per-day count·net subtotals; slim single-line rows (time-only per row — supersedes per-row date, stronger multi-day readability; compact `Sales $X · Payouts $Y`); client-side pagination 5 rows/page with Previous/Next bounds + reset-to-page-1 on every refetch; period summary line unchanged. Pure helpers exported: `groupKenoLogsByDay`, `KENO_PAGE_SIZE`. PO decisions: Keno only, page size 5, client-side paging OK (server cursor stays TD-032). Rule-28 Reds captured first (5 failing). Incident logged: python slice mis-anchor duplicated form-tail+old card — RCA'd and surgically excised (4,929 bytes) via uniqueness-guarded markers; structure verified by grep counts + green battery. 444→449/449 / 34 files; tsc=0, Biome=0, knip=0, depcruise ✔ |
Amendment A1 (PO feedback): per-day subtotal chips removed; single bold period-total banner (entries + Net $X) at top of table via keno-range-summary; headers = weekday/date labels only; 449/449 re-certified.
| M-71 | GameSales history parity (2026-08-22, PO request): M-70+A1 treatment mirrored — weekday day-group headers (no per-day totals), slim single-line rows (`name · N units @ $R = $T · h:mm a · user · actions`), client pagination 5/page with bounds+reset-on-refetch, single period-total banner (`sales-range-summary`: count / colored Total $X). Dedup per Rule 25: generic `groupLogsByDay(logs, getValue)` + `historyPageBounds` extracted to new `lib/history.ts`; Keno refactored onto it (local helpers removed). GS Reds captured first (3 failing); fixture lesson: rows need BOTH quantity_sold AND server-computed calculated_total varied for unique text matching. 449→453/453 / 34 files; tsc=0, Biome=0, knip=0, depcruise ✔ (148 modules) |
M-71 review pass (2026-08-22): containment 12/12 paths verified; helper equivalence empirically probed (grouping incl. negatives, page-boundary edges); C1 guards byte-preserved; 1 finding fixed (DayGroup dead export de-exported — knip gap) + formatting drift normalized; post-fix battery re-certified 453/453, tsc=0, knip=0, depcruise ✔.
| M-72 | Expenses history parity (2026-08-22, PO request): M-70/M-71 treatment mirrored — weekday day-group headers (no per-day totals), slim single-line rows (name · $amount · category · qty×unit · desc-truncated · h:mm a · user · badge · actions), client pagination 5/page with bounds+reset-on-refetch (visibilitychange path), single period-total banner (`expenses-range-summary`, neutral zinc-900 — outflow semantics, Rule-13 documented). Reuses lib/history.ts as-is. Reds first (4 failing). TD-053 registered: Expenses Verify button lacks in-flight guard (pre-existing C1-class). 453→457/457 / 34 files; tsc=0, Biome=0, knip=0, depcruise ✔ |
M-72 Amendment A1 (PO feedback): page grid parity — form+history now in lg:grid-cols-3 wrapper matching Keno/GameSales; categories card stays below; 457/457 re-certified.
| M-73 | Expense-category management moved to Admin/"Settings" page (2026-08-22, PO request): Manage Categories card + create/rename-with-reason/deactivate/reactivate handlers relocated Expenses→Admin; Expenses keeps read-only categories fetch feeding the entry-form dropdown (+EXPENSE_CATEGORY_FALLBACKS); Admin loader shape-guarded against non-category payloads. Reds first (8 failing: 7 migrated + absence probe). Legacy Admin tests hardened for fetch interleaving: 5 single-URL stubs + 8 ResolvedValueOnce chains → URL/method-aware fresh-response stubs; 2 call-index lookups → method-based finds; 1 fragile toHaveBeenCalledTimes(1) proxy → explicit zero-POST filter (intent preserved). 458/458 / 34 files; tsc=0, Biome=0, knip=0, depcruise ✔ |
| M-74 | Employee hiring form moved to Admin Settings + /admin widened to managers (2026-08-22, PO decisions: form-only move; managers regain hiring): Add Store Employee card relocated EmployeeRoster→Admin (success path drops local roster append — no list on Admin); roster page keeps list/edit flows; App.tsx splits /admin route into manager-visible gate while audit-logs/users stay admin-gated; Layout nav roles widened. Reds first (7 failing). Test hardening: no-token scenario uses total token-null override with guaranteed restore (mount-time GETs consume Once chains) + firebase/auth signOut mock; absence probe given explicit fetch stub. 458→459/459 / 34 files; tsc=0, Biome touched=0, knip=0, depcruise ✔ |
Consolidated review pass M-72..74 (2026-08-22): footprint 16/16 manifest-exact, zero scratch/litter; relocation containment grep-proven (single-owner UI strings, dropdown fetch intact, roster refs clean); route gates read-verified; 1 stale comment fixed; battery re-certified 459/459.
| M-75 | TD-025 closed (2026-08-22): CreateUserSchema.role removed (body role was always ignored — invite/root authoritative; dead attack surface); decisive anti-escalation test (staff invite + tampered role:admin → created staff); existing-user re-registration → clean 400. Blunt route gate attempted first, reverted after RCA (broke self-registration/root bootstrap, 8 reds). usersController 35/35 |
| M-76 | TD-026/TD-035 closed (2026-08-22): CreateSaleSchema requires game_id & drops calculated_total; createSale fetches rate pre-transaction (400 Invalid game on miss) and persists server-authoritative rate_applied/total/game_name — client values ignored (tamper-proofed). Keno half covered-by-design (M-66/M-68). WAKE metatest unblocked by restoring canonical '## 3. Scope & Boundaries' header + SYSTEM_CONTEXT mission pointer sync (was stale at M-66). Battery 463/463 / 34 files; tsc=0, knip=0, depcruise ✔ |
| — | Consolidated review pass M-72..M-74 recorded earlier this session (footprint 16/16, containment probes, battery re-certified) |
M-76 review pass + Amendment A1 (2026-08-22): probe found updateSale PUT bypass of TD-026 fix → hardened symmetrically (effective-rate resolution incl. inherited game_id, price×qty recompute, client money ignored, Invalid-game catch-map to 400); golden PUT upgraded w/ tx-payload capture; ghost-rate edit negative added; footprint re-verified 10 paths; battery 464/464.
| M-78 | Range guards + history loading feedback (2026-08-22, PO pick; TD-047/048): all three entry pages — From>To blocks fetch w/ inline hint "From date must be on or before To"; Keno/GS gain Today-reset button + Apply disabled when invalid/in-flight; Expenses effect-loader early-returns + inline spinner. listLoading drives Loader2 (testid history-loading) on all pages. Reds first 6/6. Test lessons: safeJson requires JSON content-type header on raw Responses; GS dual mount-GETs need URL-branched deferred stubs (Body-already-read); TL getNodeText only matches an element's DIRECT text — GS slim-row anchor swapped to banner testid. 470/470 / 34 files; tsc=0, Biome=0, knip=0, depcruise ✔ |
M-78 review pass (2026-08-22): 9/9 footprint, additive-only page diffs, guard-order + hint/testid consistency grep-proven, probe litter zero; refinement: Today disabled while in-flight; battery re-certified 470/470.
| M-86 | Debt Closure Sweep (2026-08-25, PO-approved 9-item closure): A — security hardening TD-010/011/012 (CORS allowlist via CORS_ORIGINS env w/ loopback defaults; helmet; two-layer express-rate-limit with mutation budget); B — ACP-008 contract additions TD-051/TD-052 (GameSalesLog.verified?/unit_type?, transactional verifySale mirroring keno, server-authoritative unit_type persistence w/ FieldValue.delete convergence + sentinel-scrubbed audit per M-66 precedent, unit-aware rows w/ legacy fallback); C — TD-050 six-button aria-label sweep + selector migrations; TD-053 Expenses C1 guards; D — TD-044 closed no-repro (6 subset configs green at HEAD); E — tsx bump clears esbuild advisory (audit 9→8; residual = TD-013 breaking majors). Review pass: audit-set() sentinel illegality caught in trace and fixed Red-Green-first; biome drift + dead bindings + unused import normalized to zero-warning standard. Battery 506/506 / 35 files coverage-gated; tsc=0 both packages; Biome touched=0; knip=0; depcruise ✔ 149 modules/448 deps; E2E 6/6 live-stack; blast radius docs/reports/M-86_Blast_Radius_Report.md |

| M-87 | Active Debt Resolution by priority (2026-08-25, PO-approved): A Firebase init hardening (TD-021/022: env-first credential resolution + eager actionable FirebaseConfigError); B deploy path (TD-019 static+SPA w/ JSON API-404; TD-023 root build/start incl. shared dist entry fix caught by boot probe; TD-018 multi-slim Dockerfile empirically probed incl. credential mount); C pagination (TD-032: opt-in limit/cursor envelope on 4 endpoints mirroring auditLogs, tolerant client reader + Load-older x4, 16-test server matrix); D TD-041 employee DELETE (ADMIN, reason, audit) + TD-036 RBAC E2E 2->5; E pino logger replacing 57 console sites (TD-024, redaction+levels tested); F breaking majors (firebase-admin@14 named-subpath migration across init; uuid override applied but nested placement npm-blocked -> TD-013 rewritten upstream-blocked at 6 moderates). SDK-14 fallout fixed: implicit-any annotations (~100), auth.test real-firebase import per house mock. Battery 549/549 / 39 files coverage-gated; tsc x2; biome touched=0; knip 0; depcruise 161 modules/480 deps; E2E 9/9 |

